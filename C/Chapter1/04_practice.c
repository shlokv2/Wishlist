#include <stdio.h>

int main()
{
    int principal = 10000, rate = 8, years = 2;
    int SimpleInterest;
    printf("The value of simple interest is %d\n", SimpleInterest = principal * rate * years / 100);
        return 0;
}